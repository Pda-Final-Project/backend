spring:
  application:
    name: common

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

  cloud:
    discovery:
      enabled: false

common:
  redis:


    enabled: false
  kafka:
    enabled: false
  discovery:
    enabled: false
