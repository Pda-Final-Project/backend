spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://userdb-cluster.db.svc.cluster.local:3306/finpagodb?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000

  kafka:
    bootstrap-servers: kafka.kafka.svc.cluster.local:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: create
    database: mysql
    open-in-view: false

common:
  redis:
    enabled: true
    password: CTg0n49k0M
    client-type: lettuce
    read-from: replica
    cluster:
      nodes:
        - 192.168.3.156:6379  # Master 1
        - 192.168.51.134:6379 # Master 2
        - 192.168.52.187:6379 # Master 3
      max-redirects: 3

jwt:
  secret: "greenlemonTgreenlemonTgreenlemonTgreenlemonTgreenlemonTgreenlemonTgreenlemonT"
  expiration: 86400000

feign:
  client:
    config:
      order-service:
        url: http://order-service.default.svc.cluster.local